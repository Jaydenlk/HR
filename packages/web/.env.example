# 前端环境变量模板。复制为 .env.local 后按需填写。
# 后端 API 基础地址(默认 http://localhost:3002/api)
NEXT_PUBLIC_API_URL=http://localhost:3002/api
# 测试通道开关:置 1 在登录页渲染「测试通道」折叠区(免验证码免邀请码,联调用)
# ⚠️ 试运行/生产环境严禁置 1(后端 DEV_LOGIN 同步保持 0)
NEXT_PUBLIC_DEV_LOGIN=0
