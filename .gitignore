node_modules/
dist/
.next/
out/
*.env
*.env.local
# .env.production 不以 .env 结尾,*.env 规则匹配不到,需显式忽略(防生产密钥误提交)。
.env.production
*.env.production
!ports.env
.DS_Store
*.db
# 备份产物(packages/api/scripts/backup.sh 输出):pg 自定义格式 dump 与 sqlite gzip 备份,严禁入库。
packages/api/backups/
# 本地/测试上传产物(FilesService UPLOAD_DIR 默认 ./uploads):用户文件,严禁入库。
packages/api/uploads/
*.dump
*.db.gz
.worktrees/
.claude/worktrees/
.claude/agent-memory/
.clone/
.audit/
.tools/*
!.tools/
!.tools/xhs-bridge.mjs
.playwright-mcp/
package-lock.json
data/we-mp-rss/
*.png
*.jpg
*.jpeg
CV/

# dev-server 本地运行残留(pid/日志)与一次性预览页,严禁入库。
# (coach-preview.db 已被上面的 *.db 覆盖,不重复列。)
.dev-server*
.admin-batch-preview/
/packages/web/src/app/roller-preview/
/packages/web/src/app/report-preview/

# 本地工件,永不入库
.claude/plans/cv-synthetic-gen.js

# 本地区:密钥/个人文件/历史截图,永不入库
_local/
