# 构建上下文裁剪:排除宿主机产物,避免污染镜像内 pnpm 安装的 node_modules / 编译产物。
# 关键:不排除 node_modules 会让宿主 node_modules(含平台特定的 native 二进制)覆盖镜像内版本。
**/node_modules
**/dist
**/.next
**/out
**/.git
**/.worktrees
**/.clone
**/.audit
**/.tools
**/.playwright-mcp
**/coverage
**/*.db
**/*.log
**/*.png
**/*.jpg
**/*.jpeg
# 生产密钥文件绝不进镜像。
.env.production
**/.env
**/.env.local
# 部署文档/本地脚本无需进镜像。
**/*.md
# CHANGELOG 需进镜像供 api 运行时读取(从最近更新生成公告),豁免上面的 **/*.md 排除。
!CHANGELOG.md
# restricted 职业资产(受限编制/隔离清单)绝不进镜像:build-context 阶段物理排除,三条各司其职——
# 裸目录模式=目录本体+全部内容整体出 context(修复:/** 只排内容,空目录节点仍随 COPY data 进镜像);
# /** 与 *-restricted.csv 两条保留作冗余防御(分别兜内容级与旧命名散落)。
data/occupations/restricted
data/occupations/restricted/**
**/*-restricted.csv
